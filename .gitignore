__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
*.so
.env
data/

# Per-run logs from benchmark/run_nlq.py, rubric_judge.py, smoke_load.py.
# JSON results in results/ are versioned; the raw log streams are not.
results/arxiv-runs/

# HERB benchmark local sandbox (isolated nexus state — never committed)
.herb-sandbox/
.venv/
# results/herb/*.json are versioned telemetry (committed); the binary HERB
# pickles consumed by HERB/code/evaluate.py are intermediates.
results/herb/*.pk

# Serena tool state
.serena/
